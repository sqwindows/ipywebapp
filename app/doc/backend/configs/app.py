debug = True
autoLoad = {
    'isLoadCache': True,  # 是否加载缓存模块
    'isLoadEvent': True,  # 是否加载事件模块
    'isLoadORM': True,  # 是否加载ORM模块
    'loadConfigsEnable': True,  # 是否加载应用层配置(configs)目录
    'loadPrelodsEnable': True,  # 是否加载应用层预载入(preloads)目录
    'loadCommandsEnable': True,  # 是否加载应用层命令(commands)目录
    'loadControllersEnable': True,  # 是否加载应用层控制器(controllers)目录
    'loadEventsEnable': True,  # 是否加载应用层事件(events)目录
    'loadSoccketsEnable': True,  # 是否加载应用层socket(sockets)目录
    'loadQuequesEnable': True,  # 是否加载应用层队列(queues)目录
    'loadAddonsEnable': True,  # 是否加载应用层插件(addons)目录
    'loadWindowEventsEnable': True,  # 是否加载应用层窗体事件(windowEvents)目录
    'loadCrontabsEnable': True,  # 是否加载应用层计划任务(crontabs)目录
    'loadTimersEnable': True,  # 是否加载应用层定时器(loadTimersEnable)目录
}
